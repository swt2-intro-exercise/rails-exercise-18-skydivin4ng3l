<h1><%= @author.name %>'s Details</h1>
<p>
  <strong>First Name:</strong>
  <%= @author.first_name %>
</p>
 
 <p>
  <strong>Last Name:</strong>
  <%= @author.last_name %>
</p>

<p>
  <strong>Homepage:</strong>
  <%= @author.homepage %>
</p>

<p>
  <strong>Papers:</strong>
  <table id= "papers_table">
    <% @author.papers.each do |paper| %>
      <tr>
        <td><%= paper.title %></td>
        <td><%= link_to "Show paper", paper_path(paper) %></td>
      </tr>
    <% end %>
  </table>
</p>
<%= link_to 'Edit', edit_author_path(@author) %></td>
<%= link_to 'Back', authors_path %>